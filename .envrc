# WSL/Linux/macOS: the Nix flake is authoritative for the toolchain.
# Disable mise's management of tools declared in mise.toml so we don't
# get duplicate installs or PATH-ordering surprises. (mise.toml exists
# for Windows-native dev, where Nix can't run.)
export MISE_DISABLE_TOOLS=go

use flake .
